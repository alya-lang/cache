[package]
name = "cache"
version = "0.1.0"
alya-version = "0.0.19"
entry = "src/lib.alya"
description = "High-performance in-memory cache with LRU/LFU/FIFO eviction, TTL expiry, hooks, snapshots, and statistics for Alya"
authors = ["Alya Language Contributors <https://github.com/alya-lang>"]
license = "MIT"
homepage = "https://github.com/alya-lang/alya"
repository = "https://github.com/alya-lang/cache"
keywords = ["alya", "alya-lang", "package", "cache", "lru", "fifo", "ttl", "memoize", "in-memory"]

[dependencies]

# [build]
# c-sources = ["c/source.c"]
# c-flags = ["-O2"]
# c-include-dirs = ["c"]
# links = "cache"

# [lint]
# disabled_rules = []
# exclude = []

# [fmt]
# exclude = []

# [test]
# exclude = []

# [bench]
# exclude = []
